<%if @success%>
	<% link = link_to "上架",on_sale_admin_product_path(@product),:id=>"product#{@product.id}",:remote=>true%>
	$("#product<%=@product.id%>").html('<%=link%>');
<%else%>
	alert('购买失败!');
<%end%>